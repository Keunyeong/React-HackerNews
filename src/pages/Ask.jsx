import styled from "styled-components";
import MainList from "../components/MainList";

const Main = styled.div``;

const Ask = () => {
  return (
    <Main>
      <MainList></MainList>
    </Main>
  );
};

export default Ask;
